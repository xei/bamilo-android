<?xml version="1.0" encoding="UTF-8"?><!-- ATTENTION: Auto increment of version code via build.xml -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ############# AD4Push: MESSAGE (Receive) ############# -->
    <permission
        android:name="com.bamilo.android.live.permission.C2D_MESSAGE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.bamilo.android.live.permission.C2D_MESSAGE" />

    <!-- ######################################## -->
    <!-- ############# Application ############## -->
    <!-- ######################################## -->

    <application
        android:name="com.mobile.app.JumiaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jumia">

        <service
            android:name="com.ad4screen.sdk.A4SService"
            android:label="A4S Service"
            android:process=":A4SService" >

            <meta-data
                android:name="com.ad4screen.no_geoloc"
                android:value="true" />
        </service>

        <!-- ############# AD4Push: Push Notifications ############# -->

        <receiver
            android:name="com.ad4screen.sdk.GCMHandler"
            android:permission="com.google.android.c2dm.permission.SEND">
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
                <action android:name="com.google.android.c2dm.intent.REGISTRATION" />
                <!-- WARNING: No use value from strings.xml, attribute is not a string value -->
                <category android:name="com.bamilo.android.live" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
