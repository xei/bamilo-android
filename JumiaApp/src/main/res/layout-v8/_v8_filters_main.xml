<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <LinearLayout
        android:layout_above="@+id/buttons"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_weight="0.4"
        android:orientation="vertical"
        >

        <!-- TITLE -->
        <com.mobile.components.customfontviews.TextView
            android:id="@+id/filter_title"
            style="@style/HeaderStyle"
            android:text="TITLE"

            />

        <View style="@style/HorizontalListDivider_black400" />

        <FrameLayout
            android:id="@+id/dialog_filter_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
           />

        </LinearLayout>

        <View style="@style/VerticalListDivider_black_400" />

        <ListView
            android:id="@+id/filters_key"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:divider="@color/black_400"
            android:background="@color/black_300"
            android:footerDividersEnabled="true"
            android:dividerHeight="1dp"/>
    </LinearLayout>

    <!-- BUTTONS -->

    <include android:id="@+id/buttons"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:layout_alignParentBottom="true"
        layout="@layout/filter_bottom_buttons"/>
</RelativeLayout>