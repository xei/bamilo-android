<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_centerVertical="true"
    android:background="@android:color/transparent"
    android:layout_centerInParent="true"
    android:layout_height="wrap_content">


    <ImageView
        android:id="@+id/tab_cart_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="5dp"
        android:layout_centerHorizontal="true"
        android:src="@drawable/btn_cart" />
    <!--selector_navigation_cart-->
    <com.mobile.components.customfontviews.TextView
        android:id="@+id/tab_cart_number_products"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:gravity="right"
        android:singleLine="true"
        android:layout_alignTop="@id/tab_cart_image"
        android:layout_alignRight="@id/tab_cart_image"
        android:textSize="10sp"
        android:paddingRight="3.5dp"
        android:text="8"
        android:textColor="@color/white"
        android:visibility="visible" />


    <com.mobile.components.customfontviews.TextView
        android:id="@+id/tab_cart_text"
        style="@style/TextCaption"
        android:layout_width="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:layout_below="@id/tab_cart_image"
        android:text="@string/cart_label"
        android:textColor="@color/white"
        android:layout_centerHorizontal="true"
        android:visibility="visible" />
</RelativeLayout>