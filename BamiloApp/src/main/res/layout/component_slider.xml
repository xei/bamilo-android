<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/clSliderRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/gray_10">

    <com.mobile.components.viewpager.PreviewViewPager
        android:id="@+id/vpSlider"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="0dp"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:paddingLeft="48dp"
        android:paddingRight="48dp"
        android:paddingTop="@dimen/dimen_16dp"
        android:visibility="visible"
        app:layout_constraintDimensionRatio="h,16:7"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <com.mobile.components.infiniteviewpager.InfiniteCirclePageIndicator
        android:id="@+id/indicatorSlider"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="4dp"
        android:layout_marginTop="4dp"
        app:fillColor="@color/orange_3"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/vpSlider"
        app:pageColor="@color/gray_5" />

</android.support.constraint.ConstraintLayout>