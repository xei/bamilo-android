<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/llCategoryDropDownParent"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:orientation="vertical"
    android:paddingTop="5dp">

    <android.support.v7.widget.CardView
        android:id="@+id/cvCategoryDropDown"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/touch_feedback"
        android:clickable="true"
        android:foreground="@drawable/touch_feedback"
        app:cardCornerRadius="1dp"
        app:cardElevation="2dp"
        app:cardUseCompatPadding="true">

        <View
            android:id="@+id/menuAnchor"
            android:layout_width="match_parent"
            android:layout_height="0dp" />

        <com.mobile.components.customfontviews.TextView
            android:id="@+id/tvCategoryDropdownTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:drawableLeft="@drawable/ic_arrow_down_gray"
            android:gravity="right|center_vertical"
            android:paddingBottom="10dp"
            android:paddingLeft="?android:attr/listPreferredItemPaddingLeft"
            android:paddingRight="?android:attr/listPreferredItemPaddingRight"
            android:paddingTop="10dp"
            android:text="@string/all_categories"
            android:textColor="@color/extradarkgrey"
            android:textSize="12sp" />

    </android.support.v7.widget.CardView>

</LinearLayout>