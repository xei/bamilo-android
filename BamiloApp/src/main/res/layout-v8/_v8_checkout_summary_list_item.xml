<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/margin_padding_l"
    android:gravity="center"
    android:layoutDirection="ltr"
    tools:ignore="UnusedAttribute">

    <!-- IMAGE CONTAINER -->
    <include
        android:id="@+id/item_image_container"
        layout="@layout/shopping_basket_item_image"
        android:layout_width="@dimen/checkout_cart_item_image"
        android:layout_height="@dimen/checkout_cart_item_image"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_gravity="center_vertical" />

    <!-- INFO CONTAINER -->

    <include
        layout="@layout/checkout_summary_product_element"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toLeftOf="@id/item_image_container" />
</RelativeLayout>